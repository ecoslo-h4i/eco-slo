import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const cardo = localFont({
  src: [
    { path: "./fonts/cardo/Cardo-Regular.ttf", weight: "400", style: "normal" },
    { path: "./fonts/cardo/Cardo-Italic.ttf", weight: "400", style: "italic" },
    { path: "./fonts/cardo/Cardo-Bold.ttf", weight: "700", style: "normal" },
  ],
  display: "swap",
  variable: "--font-cardo",
});

//! Update metadata to match your project
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

const avenir = localFont({
  src: [
    {
      path: "../../public/fonts/Avenir/Avenir-Regular.woff2",
      weight: "400",
      style: "normal",
    },
    {
      path: "../../public/fonts/Avenir/Avenir-Bold.woff2",
      weight: "700",
      style: "normal",
    },
  ],
  display: "swap",
  variable: "--font-avenir",
});

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${cardo.variable} ${avenir.variable} font-avenir`}>{children}</body>
    </html>
  );
}
