import { AdminPageShell } from "@/components/admin-page-shell";

export default function Settings() {
  return (
    <AdminPageShell title="Settings">
      <div className="rounded-xl border border-border bg-muted-card-bg p-6 font-mulish text-text-muted">
        Settings are not configured yet.
      </div>
    </AdminPageShell>
  );
}
