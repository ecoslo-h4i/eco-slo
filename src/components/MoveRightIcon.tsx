import { MoveRight } from "lucide-react";

export default function MoveRightIcon({ className }: { className?: string }) {
  return <MoveRight aria-hidden="true" className={`${className} inline-block`} strokeWidth={2} />;
}
